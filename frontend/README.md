# Frontend translator UI

Static UI for the streaming translator. Build the container and open http://localhost:8080.

## Docker

```bash
docker build -t akkadian-frontend .
docker run --rm -p 8080:8080 akkadian-frontend
```
